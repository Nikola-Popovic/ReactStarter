import React from 'react';
import { Button, ButtonProps } from '@mui/material';

type SkolaButtonProps = ButtonProps;

export const SkolaButton = (props: SkolaButtonProps) => {
  return <Button {...props}>
  </Button>;
};
